from django.apps import AppConfig


class EntregaConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.entrega"
    label = "entrega"
    verbose_name = "Entrega / Instalação"
